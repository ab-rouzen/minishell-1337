#include "minishell.h"
int main()
{
    char word[] = "abcdef";  
    x+=0
    0123
    int idxToDel = 2; 
    memmove(&word[idxToDel], &word[idxToDel + 1], strlen(word) - idxToDel);
    printf("%s", word);
}